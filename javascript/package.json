{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "workspaces": [
    "packages/*",
    "targets/*/*"
  ],
  "scripts": {
    "build": "lerna run tsc && lerna run build"
  }
}
